{
  "private": true,
  "name": "preact-cli-ssr-demo",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "preact watch",
    "build": "preact build",
    "start": "node server-main.js",
    "prodstart": "preact build && rm -f build/index.html && node server-main.js"
  },
  "devDependencies": {
    "assets-webpack-plugin": "^3.9.10",
    "preact-cli": "^2.0.0"
  },
  "dependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "compression": "^1.7.1",
    "preact": "^8.2.1",
    "preact-compat": "^3.17.0",
    "preact-render-to-string": "^3.7.0"
  }
}
